import SwiftUI

struct DatesImportantes: View {
    var body: some View {
        ScrollView {
            VStack(spacing: 20) {
                Text("TEXTE")
                    .font(.title)
                    .padding()

 // Si tu as une vue calendrier personnalisée
            }
            .padding()
        }
        .navigationTitle("Dates Importantes")
    }
}
